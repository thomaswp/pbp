const array = $$array;
// TODO: Begin loop
    // TODO: Get the item and index of each item in the array
    processItem(item, index);
// End loop

function processItem(item, index) {
    console.log(`Item ${index} of list is ${item}`);
}